# This file makes the 'routers' directory a Python package.
from fastapi import APIRouter

# You can optionally define a main router for this package here
# and include other specific routers into it.
# For example:
# router = APIRouter()
# from . import users, items
# router.include_router(users.router)
# router.include_router(items.router)

# Or just leave it empty if routers are included directly in main.py
